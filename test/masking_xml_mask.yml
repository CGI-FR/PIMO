version: "1"
seed: 42
masking:
  - selector:
      jsonpath: "content"
    mask:
      xml:
        xpath: "note"
        masking:
          - selector:
              jsonpath: "@author"
            mask:
              template: "New Author Name"
